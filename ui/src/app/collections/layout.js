import 'bootstrap/dist/css/bootstrap.css';
import "@/app/globals.css";

export const metadata = {
  title: "Collections Diff/Merge",
  description: "Collections Diff/Merge",
};

export default function CollectionsLayout({ children }) {
  return (
      <>{children}</>
  );
}
